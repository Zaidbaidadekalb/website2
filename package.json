{
  "name": "key-management-dashboard",
  "version": "1.0.0",
  "description": "Key Management Dashboard",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "build": "echo 'Building...' && mkdir -p build && cp -r index.html app.js build/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["key", "management", "dashboard"],
  "author": "Your Name",
  "license": "ISC",
  "dependencies": {
    "@cloudflare/kv-asset-handler": "^0.3.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
